---
phase: 1
plan: 2
title: "Public Portfolio Frontend"
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
  - src/app/(public)/about/page.tsx
  - src/app/(public)/services/page.tsx
  - src/app/(public)/industries/page.tsx
  - src/app/(public)/contact/page.tsx
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/Stats.tsx
  - src/components/sections/ServicesOverview.tsx
  - src/components/sections/Testimonials.tsx
  - src/components/sections/CTASection.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/SectionHeading.tsx
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03]
---

# Plan 01-02: Public Portfolio Frontend

<objective>
Build the full public-facing marketing website for Primetek Global Solutions — Home, About, Services, Industries, and Contact pages. Every page must be responsive, SEO-optimized, and visually premium with micro-animations and smooth scrolling.
</objective>

<must_haves>
- Home page with Hero CTA, animated stats counter, services overview, testimonials carousel, and contact CTA
- About page with company story, vision/mission, and leadership team section
- Services page with detailed staffing, consulting, and outsourcing service cards
- Industries page showcasing IT, Healthcare, Finance, Manufacturing, and Retail verticals
- Contact page with structured inquiry form (Name, Email, Company, Requirement)
- Responsive Navbar with mobile hamburger menu and smooth scroll navigation
- Footer with company info, quick links, and social media placeholders
- All pages server-rendered for SEO
</must_haves>

## Tasks

<task id="01-02-01">
<title>Create reusable UI components (Button, Card, SectionHeading)</title>
<read_first>
- src/lib/utils.ts
- src/app/globals.css
</read_first>
<action>
Create `src/components/ui/Button.tsx`:
```typescript
'use client';
import { forwardRef } from 'react';
import { cn } from '@/lib/utils';

interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
}

const Button = forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant = 'primary', size = 'md', children, ...props }, ref) => {
    return (
      <button
        ref={ref}
        className={cn(
          'inline-flex items-center justify-center font-medium rounded-lg transition-all duration-250 focus:outline-none focus:ring-2 focus:ring-offset-2',
          {
            'bg-primary-500 text-white hover:bg-primary-600 focus:ring-primary-400 shadow-lg hover:shadow-xl': variant === 'primary',
            'bg-navy-800 text-white hover:bg-navy-700 focus:ring-navy-600': variant === 'secondary',
            'border-2 border-primary-500 text-primary-500 hover:bg-primary-50 focus:ring-primary-400': variant === 'outline',
            'text-primary-500 hover:bg-primary-50': variant === 'ghost',
          },
          {
            'px-4 py-2 text-sm': size === 'sm',
            'px-6 py-3 text-base': size === 'md',
            'px-8 py-4 text-lg': size === 'lg',
          },
          className
        )}
        {...props}
      >
        {children}
      </button>
    );
  }
);

Button.displayName = 'Button';
export default Button;
```

Create `src/components/ui/Card.tsx` — A glassmorphic card with hover lift effect:
```typescript
import { cn } from '@/lib/utils';

interface CardProps {
  children: React.ReactNode;
  className?: string;
  hover?: boolean;
}

export default function Card({ children, className, hover = true }: CardProps) {
  return (
    <div className={cn(
      'rounded-xl bg-white/80 backdrop-blur-sm border border-gray-100 p-6 shadow-sm',
      hover && 'transition-all duration-300 hover:shadow-xl hover:-translate-y-1',
      className
    )}>
      {children}
    </div>
  );
}
```

Create `src/components/ui/SectionHeading.tsx`:
```typescript
interface SectionHeadingProps {
  label?: string;
  title: string;
  description?: string;
  centered?: boolean;
}

export default function SectionHeading({ label, title, description, centered = true }: SectionHeadingProps) {
  return (
    <div className={cn('mb-12', centered && 'text-center')}>
      {label && (
        <span className="inline-block text-primary-500 font-semibold text-sm uppercase tracking-wider mb-2">
          {label}
        </span>
      )}
      <h2 className="text-3xl md:text-4xl lg:text-5xl font-heading font-bold text-navy-900 mb-4">
        {title}
      </h2>
      {description && (
        <p className="text-lg text-text-secondary max-w-2xl mx-auto">
          {description}
        </p>
      )}
    </div>
  );
}
```
</action>
<acceptance_criteria>
- src/components/ui/Button.tsx exports a Button component with variant and size props
- src/components/ui/Card.tsx exports a Card component with glassmorphic styling
- src/components/ui/SectionHeading.tsx exports a SectionHeading with label, title, description props
- All components use the `cn` utility from `@/lib/utils`
</acceptance_criteria>
</task>

<task id="01-02-02">
<title>Build Navbar and Footer layout components</title>
<read_first>
- src/components/ui/Button.tsx
- src/app/globals.css
</read_first>
<action>
Create `src/components/layout/Navbar.tsx` — A responsive navigation bar with:
- Primetek Global Solutions logo/text on the left
- Navigation links: Home, About, Services, Industries, Contact
- Mobile hamburger menu with Framer Motion slide animation
- Sticky header that adds backdrop-blur on scroll
- Active link highlighting
- "Get in Touch" CTA button on desktop

Create `src/components/layout/Footer.tsx` — A professional footer with:
- Company name and brief tagline
- Quick links column (Home, About, Services, Industries, Contact)
- Services column (IT Staffing, Consulting, Outsourcing)
- Contact info column (placeholder email, phone, address)
- Bottom bar with copyright and social media icon placeholders (Lucide icons: Linkedin, Twitter, Mail)
- Dark navy background (`bg-navy-900`) with light text

Create `src/app/(public)/layout.tsx` — Public layout wrapper:
```typescript
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';

export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <Navbar />
      <main>{children}</main>
      <Footer />
    </>
  );
}
```
</action>
<acceptance_criteria>
- src/components/layout/Navbar.tsx contains navigation links for Home, About, Services, Industries, Contact
- src/components/layout/Navbar.tsx contains a mobile menu toggle
- src/components/layout/Footer.tsx contains copyright text with "Primetek Global Solutions"
- src/app/(public)/layout.tsx wraps children with Navbar and Footer
</acceptance_criteria>
</task>

<task id="01-02-03">
<title>Build Home page with Hero, Stats, Services, Testimonials, CTA sections</title>
<read_first>
- src/components/ui/Button.tsx
- src/components/ui/Card.tsx
- src/components/ui/SectionHeading.tsx
</read_first>
<action>
Create `src/components/sections/Hero.tsx`:
- Full-viewport height hero with dark navy gradient background
- Bold heading: "Empowering Businesses with World-Class Talent"
- Subheading: "Your trusted partner for staffing, consulting, and outsourcing solutions"
- Two CTA buttons: "Get Started" (primary, links to /contact) and "Our Services" (outline, links to /services)
- Subtle particle/gradient animation using CSS
- Framer Motion fade-in-up animation on mount

Create `src/components/sections/Stats.tsx`:
- Animated counter section with 4 stats: "500+ Clients Served", "10,000+ Placements", "15+ Industries", "98% Client Satisfaction"
- Numbers animate counting up when section enters viewport (Framer Motion + useInView)
- Gold accent color for numbers

Create `src/components/sections/ServicesOverview.tsx`:
- 3 service cards (IT Staffing, Consulting, Outsourcing)
- Each card has an icon (Lucide: Users, BarChart3, Globe), title, description, and "Learn More" link
- Cards use the glassmorphic Card component with hover lift

Create `src/components/sections/Testimonials.tsx`:
- Testimonial carousel/slider with 3 sample testimonials
- Each testimonial has quote text, client name, company, and role
- Smooth auto-rotate with manual navigation dots

Create `src/components/sections/CTASection.tsx`:
- Full-width banner with gradient background
- Heading: "Ready to Transform Your Workforce?"
- CTA button: "Contact Us Today"

Compose all sections in `src/app/(public)/page.tsx`:
```typescript
import Hero from '@/components/sections/Hero';
import Stats from '@/components/sections/Stats';
import ServicesOverview from '@/components/sections/ServicesOverview';
import Testimonials from '@/components/sections/Testimonials';
import CTASection from '@/components/sections/CTASection';

export default function HomePage() {
  return (
    <>
      <Hero />
      <Stats />
      <ServicesOverview />
      <Testimonials />
      <CTASection />
    </>
  );
}
```
</action>
<acceptance_criteria>
- src/app/(public)/page.tsx renders Hero, Stats, ServicesOverview, Testimonials, CTASection
- src/components/sections/Hero.tsx contains "Empowering Businesses"
- src/components/sections/Stats.tsx contains "500+" and animated counter logic
- Home page is fully server-rendered (no 'use client' on page.tsx itself)
</acceptance_criteria>
</task>

<task id="01-02-04">
<title>Build About, Services, Industries, and Contact pages</title>
<read_first>
- src/app/(public)/page.tsx
- src/components/ui/SectionHeading.tsx
- src/components/ui/Card.tsx
</read_first>
<action>
Create `src/app/(public)/about/page.tsx`:
- Page metadata: title "About Us"
- Company story section with history paragraph
- Vision and Mission cards side by side
- Leadership team grid (3-4 placeholder team members with name, role, and generated avatar)

Create `src/app/(public)/services/page.tsx`:
- Page metadata: title "Our Services"
- Service categories: IT Staffing (Contract, Full-time, Temp-to-Hire), Non-IT Staffing, Consulting (Strategy, Digital Transformation, Process Optimization), Outsourcing (BPO, KPO, IT Outsourcing)
- Each category has a detailed card with icon, description, and key benefits list
- Framer Motion stagger animation on scroll

Create `src/app/(public)/industries/page.tsx`:
- Page metadata: title "Industries We Serve"
- Industry cards for: IT, Healthcare, Finance, Manufacturing, Retail
- Each card has an icon (Lucide), industry name, short description, and key staffing areas

Create `src/app/(public)/contact/page.tsx`:
- Page metadata: title "Contact Us"
- Left side: Contact information (email, phone, address placeholders), embedded map placeholder
- Right side: Inquiry form with fields: Name, Email, Company, Phone (optional), Requirement (textarea)
- Form uses React Hook Form + Zod (inquirySchema) for client-side validation
- Form submits to `/api/inquiries` via POST
- Success/error toast feedback after submission
</action>
<acceptance_criteria>
- src/app/(public)/about/page.tsx contains "Vision" and "Mission" text
- src/app/(public)/services/page.tsx contains "IT Staffing", "Consulting", "Outsourcing"
- src/app/(public)/industries/page.tsx contains "Healthcare", "Finance", "Manufacturing"
- src/app/(public)/contact/page.tsx contains a form with fields: name, email, company, requirement
- Contact form imports and uses inquirySchema from @/lib/validations
</acceptance_criteria>
</task>

<verification>
- All 5 public pages render without errors
- Navigation between pages works correctly
- Mobile responsive at 320px, 768px, 1024px, 1440px
- Each page has proper metadata (title, description)
- Contact form validates inputs before submission
</verification>
